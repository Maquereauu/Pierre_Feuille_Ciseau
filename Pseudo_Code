#DEBUT
#Importer module random
#définir foncion papier_caillou_ciseau avec comme paramètre multiplayer(bool)
#assigner pts_p1 est égal à 0
#assigner pts_p2 est égal à 0
#assigner dict_pcs avec comme clés les noms des différents coups et en valeur leurs description ( laquelle bat laquelle) 
#fonction input demandant si l'utilisateur veut connaître les rêgles est vrai
#si le retour de la fonction input demandant si l'utilisateur veut connaître les rêgles est vrai
#alors retourne le dictionnaire
#si multiplayer vaut 1
#alors tant que pts_p1 < 3 et pts_p2 < 3
#alors assigner p1 est égal à la valeur du retour de la fonction input demandant quel coup veut-il faire
#assigner p2 est égal à la valeur du retour de la fonction input demandant quel coup veut-il faire
#si p1 == p2
#alors affiche "Draw"
#si p1 == 0 et p2 == 1
#alors on incrémente p2_pts de 1
#si p1 == 0 et p2 == 2
#alors on incrémente p1_pts de 1
#si p1 == 1 et p2 == 0
#alors on incrémente p1_pts de 1
#si p1 == 2 et p2 == 0
#alors on incrémente p2_pts de 1
#si p1 == 2 et p2 == 1
#alors on incrémente p1_pts de 1
#si p1 == 1 et p2 == 2
#alors on incrémente p2_pts de 1
#afficher gagnant
# fonction input oui/non
#si oui
#alors retourner papier_caillou_ciseau()
#si multiplayer vaut 0 
#alors tant que pts_p1 < 3 et pts_p2 < 3
#alors assigner p1 est égal à la valeur du retour de la fonction input demandant quel coup veut-il faire
#assigner p2 est égal au retour de la fonction randint(0,2)
#si p1 == p2
#alors affiche "Draw"
#si p1 == 0 et p2 == 1
#alors on incrémente p2_pts de 1
#si p1 == 0 et p2 == 2
#alors on incrémente p1_pts de 1
#si p1 == 1 et p2 == 0
#alors on incrémente p1_pts de 1
#si p1 == 2 et p2 == 0
#alors on incrémente p2_pts de 1
#si p1 == 2 et p2 == 1
#alors on incrémente p1_pts de 1
#si p1 == 1 et p2 == 2
#alors on incrémente p2_pts de 1
#afficher gagnant
# fonction input oui/non
#si oui
#alors retourner papier_caillou_ciseau()
